<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>IOAyman</groupId>
    <artifactId>RegExpBuilderJava</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Regular Expressions Builder (Java)</name>
    <url>https://github.com/thebinarysearchtree/regexpbuilderjava</url>
    <description>RegExpBuilder integrates Regular Expressions into the programming language, thereby making them easy to
        read and maintain. Regular Expressions are created by using
        chained methods and variables such as arrays or strings.
    </description>

    <licenses>
        <license>
            <name>MIT</name>
            <url>https://github.com/thebinarysearchtree/regexpbuilderjava/blob/master/LICENSE.md</url>
        </license>
    </licenses>

    <contributors>
        <contributor>
            <name>Ayman Nedjmeddine</name>
            <url>https://github.com/IOAyman/regexpbuilderjava</url>
        </contributor>
    </contributors>


    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${artifactId}-${version}</finalName>
        <sourceDirectory>${basedir}/src/main</sourceDirectory>
        <testSourceDirectory>${basedir}/src/test</testSourceDirectory>
        <outputDirectory>${basedir}/target/classes</outputDirectory>
        <testOutputDirectory>${basedir}/target/test-classes</testOutputDirectory>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.7</source>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
